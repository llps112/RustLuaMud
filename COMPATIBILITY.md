# 兼容性策略

> 本文档声明 RustLuaMud 从 **v1.0.0** 起对用户可见接口的稳定性承诺：哪些冻结、哪些可变、变更如何处置。
> 1.0 的本质是一份承诺，本文档即该承诺的原文。**未在「冻结面」中列出的内容，不受稳定性保护。**

## 一、版本语义

| 版本位 | 含义 |
|--------|------|
| **MAJOR**（1.x → 2.0） | 破坏「冻结面」中的任何一项；需按 §五 流程弃用 |
| **MINOR**（1.0 → 1.1） | 新增功能 / 新增 API / 新增配置项，向后兼容 |
| **PATCH**（1.0.0 → 1.0.1） | 缺陷修复，不改变既定行为契约 |

**历史说明**：`0.x` 阶段不提供任何稳定性承诺——例如 v0.9.9 即调整了重连与心跳超时行为（重连竞态防护、心跳超时改由主动断开并显式排期重连）。稳定性承诺自 `1.0.0` 起生效。

## 二、冻结面（1.0 起承诺不破坏）

| 面 | 内容 | 权威来源 |
|----|------|---------|
| **Lua 脚本 API** | 已文档化的函数签名、回调参数、返回值类型 | [`help/api/`](help/api/README.md) —— **不在此处复制清单，以该目录为准** |
| **API 常量表** | `trigger_flag` / `alias_flag` / `timer_flag` / `custom_colour` / `sendto` / `error_code` / `error_desc` 等 | 逐条对齐 MUSHclient 上游源码（`lua_methods.cpp`），**禁止自行扩展或遗漏** |
| **PROFILE TOML 配置** | 字段名、默认值、取值范围、语义 | [README](README.md) 配置示例节（含各字段注释） |
| **CLI 参数** | `--version`、`--profiles <目录>`、`--daemon [stop\|status]` | [main.rs](src/main.rs) |
| **文件产物路径** | `<profiles>/daemon.pid`、`<profiles>/terminal.json`、`<profiles>/.env`、日志目录 | [daemon.rs](src/daemon.rs)、[app.rs](src/app.rs)、[config.rs](src/config.rs) |
| **日志格式** | 行结构 `[HH:MM:SS.mmm] [标签] 正文`；文件名 `{角色}_{YYMMDD_HH}.log`（如 `mud_250626_14.log`，每小时滚动） | [logger.rs](src/log/logger.rs) |
| **日志标签** | `OUT`（服务器输出）、`CMD`（脚本发令）、`LUA`（/lua）、`DBG`（调试）、`PNC`（panic）、`DCN`（断线）、`RCN`（重连） | [logger.rs](src/log/logger.rs) |
| **退出与信号行为** | 启动期配置/可写性检查失败 → 退出码 `1`；`--version` 与 `--daemon stop\|status` → `0`；看门狗超时 → **SIGABRT**（signal 6，供 systemd `Restart=on-failure` 识别） | [main.rs](src/main.rs)、[engine.rs](src/lua/engine.rs) |

> **关于日志标签的边界**：标签集合与行结构冻结；**正文措辞不冻结**（可在 PATCH 版本中改进表述）。基于日志做解析时，请匹配标签而非正文。

## 三、不冻结面（可自由变更）

- **内部实现**：Rust 模块划分、函数与类型命名、文件规模、依赖版本（含第三方库升级）
- **日志正文**：消息措辞、诊断信息详略
- **终端渲染**：界面布局、面板尺寸与样式、配色、刷新时序
- **性能特征**：吞吐、内存占用、启动耗时（目标不退化，但不作为契约）
- **私有脚本子模块**：`scripts/private` 为作者个人脚本实现，不属于公开契约，其内容与接口不受本文档约束（详见 [ROADMAP §0](ROADMAP.md)）
- **标注为实验性的功能**：上线时会在 release notes 中显式标注，其接口在转正前可变

## 四、已知偏差（文档化的限制，非缺陷）

以下行为**与 MUSHclient 不完全一致或需注意**，属有意设计，在 1.0 后不会因此视为破坏兼容：

1. **触发/别名正则基于 Rust `regex` 引擎（PCRE 语法子集）**
   自动转换 `\Z`/`\z`；**不支持反向引用与前后查找**。不兼容的模式在注册时**即时报错**（不会静默错配）。
2. **未实现的 API 返回 `""` / `0` / `false`，而非抛错**
   ⇒ **脚本调用未实现 API 时会静默降级**。自查方式：确认脚本中调用的 API 均在 [`help/api/`](help/api/README.md) 覆盖范围内（README 亦列有典型未实现项）。
3. **`--daemon` 仅 Unix**（fork/setsid/PTY 为 POSIX 机制）；Windows 侧明确不做服务化，开机自启由 `start_mud.bat` 承担。
4. **崩溃堆栈默认开启**：程序在 `RUST_BACKTRACE` 未设置时补默认值 `1`；显式设置（`0` 关闭 / `full` 完整栈）一律尊重。

## 五、变更流程（弃用策略）

若确需改动「冻结面」中的内容，按以下次序执行：

1. **标注**：在 release notes 与本文档中说明变更与理由
2. **兼容期**：保留旧行为至少一个 MINOR 版本；可行时输出告警提示旧用法（例如配置项改名时同时接受新旧字段并告警）
3. **移除**：在下一个 MAJOR 版本移除旧行为，并在 release notes 中列为破坏性变更

**例外（可立即变更，无需兼容期）**：

- 安全缺陷修复
- 依赖上游 MUSHclient 常量表变更（该表以上游为准，跟随上游即为正确）

## 六、适用范围

本文档面向**使用本客户端运行自己脚本与配置**的场景，覆盖 Lua API、配置格式、CLI 与产物路径。

**不覆盖**：私有脚本子模块的内部实现；未文档化的行为（包括内部函数、非公开 Lua 全局量、渲染细节）——依赖未文档化行为属于不受支持的用法。

---

**相关文档**：[README](README.md)（安装、配置、API 覆盖说明） · [`help/api/`](help/api/README.md)（API 权威清单） · [ROADMAP](ROADMAP.md)（定位与路线图）