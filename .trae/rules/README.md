# .trae/rules/ — 已迁移

本目录的开发规则已迁移至 `.qoder/rules/`（权威源），请勿在本目录维护规则副本，避免双副本漂移。

- 规则文件：`.qoder/rules/*.md`（8 个，含 `trigger: always_on` / `trigger: model_decision` frontmatter）
- 历史说明：本目录曾存放与 `.qoder/rules/` 内容相同的规则副本，因两处并行维护导致漂移（例如 `regex-pattern.md` 中的中文弯引号曾被编辑工具静默归一化为英文直引号），故合并为单一权威源。

修改规则请只编辑 `.qoder/rules/` 下的文件。
