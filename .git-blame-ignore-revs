# git blame 忽略列表
#
# 本文件列出「大规模纯移动 / 纯格式化」提交的完整 hash，供 git blame 穿透，
# 使每一行的归属指向真正改写该行逻辑的那次提交，而不是搬运它的那次。
#
# 启用方式（每台机器、每次重新 clone 都要各配一次，git config 不随仓库传播）：
#
#     git config blame.ignoreRevsFile .git-blame-ignore-revs
#
# GitHub 网页端的 blame 会读取仓库内同名的本文件，无需额外配置本机 git。
#
# ⚠️ 已实测的适用边界（git 2.43）：当被忽略的提交是「把若干新路径创建出来」的
# 跨文件拆分时，本机制**不生效** —— git 无法把某一行的改动「当作没发生过」，
# 因为在父提交里那条路径根本不存在，只能把行归属留给创建它的那次提交。
# 此时真正有效的是 blame 自身的拷贝检测 -C：
#
#     git blame -C src/lua/api/trigger_api.rs
#
# 本文件仍予保留：它是「此提交为纯移动」的约定登记处，且 GitHub 网页端会读取；
# 若将来出现同路径内的纯格式化提交，本机制即可正常穿透。

# refactor(lua): 按 API 族拆分注册模块，无行为变更
# src/lua/api.rs 拆为 src/lua/api/ 下 10 个子模块，纯移动。
# 查看方式用 `git blame -C <文件>`（见上方边界说明）。
4ea6ed4f144ca32018096cb1b56d1b0192f05131

# refactor(lua): 收敛 api 子模块行数至 ≤400，无行为变更
# 继续把 module_loader.rs / trigger_api.rs 按原有分节拆细，同属纯移动。
d26111f7594465f0d83d09228dfbbaf1e1a3a200
